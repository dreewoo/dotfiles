# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4


# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec urxvtc

# kill focused window
bindsym $mod+Shift+Q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -fn 'Hack-11' -nb '#000000' -nf '#999999'
bindsym $mod+Control+d exec passmenu -fn 'Hack-11' -nb '#000000' -nf '#999999'
#bindsym $mod+d exec j4-dmenu-desktop -i -nb '#000000' -nf '#999999'

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym XF86MonBrightnessDown exec xbacklight -dec 2
bindsym XF86MonBrightnessUp exec xbacklight -inc 2
bindsym XF86AudioRaiseVolume exec amixer -q -D pulse set Master 5%+ unmute%
bindsym XF86AudioLowerVolume exec amixer -q -D pulse set Master 5%- unmute
bindsym XF86AudioMute exec amixer -q -D pulse set Master toggle

bindsym XF86Explorer exec nautilus

#bindsym $mod+Left workspace prev
#bindsym $mod+Right workspace next

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

bindsym $mod+Print exec scrot
bindsym XF86TouchpadToggle exec synclient TouchpadOff=$(synclient -l | grep -c 'TouchpadOff.*=.*0')
bindsym $mod+Control+l exec i3lock -f

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+g split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

set $w1   WEB
set $w2 2: 
set $w3 3: 
set $w4 4: 
set $w5 5: 
set $w6 6: 
set $w7 7: 
set $w8   MAIL
set $w9   MEDIA
#set $w0   IRC
set $w0   IRC

bindsym $mod+1 workspace $w1
bindsym $mod+2 workspace $w2
bindsym $mod+3 workspace $w3
bindsym $mod+4 workspace $w4
bindsym $mod+5 workspace $w5
bindsym $mod+6 workspace $w6
bindsym $mod+7 workspace $w7
bindsym $mod+8 workspace $w8
bindsym $mod+9 workspace $w9
bindsym $mod+0 workspace $w0

bindsym $mod+Control+Right move workspace to output right
bindsym $mod+Control+Left move workspace to output left

#gaps inner 4
#gaps outer 2

# move focused container to workspace
bindsym $mod+Shift+exclam move container to workspace $w1
bindsym $mod+Shift+at move container to workspace $w2
bindsym $mod+Shift+numbersign move container to workspace $w3
bindsym $mod+Shift+dollar move container to workspace $w4
bindsym $mod+Shift+percent move container to workspace $w5
bindsym $mod+Shift+asciicircum move container to workspace $w6
bindsym $mod+Shift+ampersand move container to workspace $w7
bindsym $mod+Shift+asterisk move container to workspace $w8
bindsym $mod+Shift+parenleft move container to workspace $w9
bindsym $mod+Shift+parenright move container to workspace $w0
# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

set $mode_system System [l]ock, l[o]gout, [s]uspend, [h]ibernate, [r]eboot, shut[d]own
mode "$mode_system" {
    bindsym l exec --no-startup-id i3lock -i /home/jocelyn/.walp.png, mode "default"
    bindsym o exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id systemctl suspend, mode "default"
    bindsym h exec --no-startup-id systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym d exec --no-startup-id systemctl poweroff, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Pause mode "$mode_system"

set $mode_xrandr xrandr [c]lone, [l]eft, [r]ight, [o]ff
mode "$mode_xrandr" {
    bindsym c exec /home/jocelyn/.xrandr --same-as, mode "default"
    bindsym l exec /home/jocelyn/.xrandr --left-of, mode "default"
    bindsym r exec /home/jocelyn/.xrandr --right-of, mode "default"
    bindsym o exec /home/jocelyn/.xrandr --off, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym XF86Display mode "$mode_xrandr"


bindsym $mod+r mode "resize"

#bindsym $mod+Shift+ exec i3lock

#bindsym $mod+t border toggle
new_window 1pixel
#floating_maximum_size 1550 x 850

for_window [class="ioquake3"] floating enable
#for_window [class="(?i)chromium"] floating enable
for_window [class="(?i)pavucontrol"] floating enable
for_window [class="(?i)mpv"] floating enable
for_window [class="(?i)uberwriter"] floating enable
for_window [class="(?i)steam"] floating enable
for_window [class="notify"] floating enable
for_window [window_role="pop-up"] floating enable

assign [class="^mpv$"] → $w9
assign [title="irc"] → $w0
assign [Class="slack"] → $w0
assign [Class="Evolution$"] → $w8
assign [Class="Chromium"] → $w1

# class                 border  backgr. text    indicator
client.focused          #002b36 #131313 #ffffff #2e9ef4
client.focused_inactive #002b36 #131313 #ffffff #484e50
client.unfocused        #002b36 #131313 #888888 #292d2e
client.urgent           #2f343a #900000 #ffffff #900000

hide_edge_borders both
#bindsym $mod+t border normal

bindsym $mod+m move scratchpad
bindsym $mod+o scratchpad show

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    font pango:Sauce Code Powerline, FontAwesome 9
    position bottom
    #mode hide
#    status_command $HOME/.i3/conky-i3bar
    status_command i3status
    colors {
        separator  #666666
        background #000000
        #background #131313
        statusline #888888
        focused_workspace  #000000 #000000 #cb4b16
        active_workspace   #000000 #000000 #dddddd
        inactive_workspace #000000 #000000 #888888
        urgent_workspace   #000000 #000000 #d33682
    }
}

exec --no-startup-id nm-applet
exec --no-startup-id udiskie -s
exec --no-startup-id redshift-gtk -l 48.5:2.2 -t 6000:4500
#exec ntimed-client -t /tmp/ntimed.trace 0.fr.pool.ntp.org 1.fr.pool.ntp.org &
#exec --no-startup-id pa-applet

#exec i3bar -c conky -f "-*-terminus-medium-r-*-*-12-*-*-*-*-*-*-*" -dtop --color-bar-fg "#aaaaaa" --color-bar-bg "#262626" --color-active-ws-fg "#ffffff" --color-active-ws-bg "#262626" --color-inactive-ws-fg "#aaaaaa" --color-inactive-ws-bg "#262626" --color-urgent-ws-bg "#ffffff" --color-urgent-ws-fg "#262626" --color-focus-ws-fg "#ffffff" --color-focus-ws-bg "#262626"

